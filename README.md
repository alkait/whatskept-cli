# whatskept

## Dev

```
go build          # produces ./whatskept
./whatskept -h
./whatskept init [dir]   # initialize a workspace
./whatskept import <ios-backup-path>   # import (validation + device binding so far)
./whatskept import --list   # list iOS backups on this machine
go test ./...            # run all tests
```
